# Plan: Investigate / remove Temporal Workflow

Goal: Identify when Temporal Workflow was added and plan steps to revert to the general approach.

Context / findings:
- composer.json includes `"temporal/sdk": "^v2.5.4"`
- docker-compose.yml / compose.yaml has temporal services (postgresql, temporal, admin-tools, web, ui)
- Git history shows temporal added via commits:
  - 761545f Roadrunner + temporal
  - 3654298 / fbef7ae / 62805f6 / d21c733 / 5c1e854 / 6db641f / 5802528 / 4794fd0
  - Key: 5c1e854 "Warehouse service in temporal", 6db641f "Erp + Warehouse new order created request via temporal", 5802528 "Daily Sales Report via temporal"
- Source files referencing temporal need identification.

General approach (confirmed): sync controllers + Laravel Horizon (Redis) for Laravel Jobs.

Steps (preparation + removal plan):
0. `git checkout -b dev2 dev` (new branch from dev).
1. Merge `vue3` branch into `dev2`: `git merge vue3` (resolve conflicts, keep business logic).
2. Compare `master` branch package updates: `git diff master -- package.json composer.json` or inspect master lockfiles; update packages accordingly (`npm update`, `composer update`) — apply only updates available on master that are safe.
3. List source files using temporal SDK (`grep -rni temporal app/`).
4. Read git diffs for key commits (`761545f`, `5802528`, `5c1e854`, `6db641f`).
5. Identify temporal workflow/activity/worker classes.
6. Replace with sync controllers + Laravel Jobs; keep current business logic unchanged; only change delivery mechanism (not rules/logic).
7. Remove `temporal/sdk` from composer.json, remove temporal services from docker-compose.yml / compose.yaml, delete temporal source files/config.
8. Add/configure Horizon + Redis if missing; verify (`php artisan horizon`, `npm run test`).

Risks / tradeoffs:
- Temporal long-running workflows need equivalent Laravel Job + retry logic.
- Redis/Horizon setup required before removal.

Next: Execute removal and migration steps.
